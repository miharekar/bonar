<div class="filter" id="filter">
  <a href="#" class="close-menu">zapri</a>
  <ul>
    <li><input type="search" id="restaurantSearch" placeholder="Išči" incremental="incremental"></input></li>
    <li><input type="checkbox" name="all" id="all_switch" checked="checked" disabled="disabled"><label for="all_switch">vse</label></li>
    <% @features.each do |feature| %>
    <li><input type="checkbox" name="celiac" class="feature-filter" id="<%= feature.title.gsub(' ', '') %>_switch"  data-feature-id="<%= feature.id %>"><label for="<%= feature.title.gsub(' ', '') %>_switch"><%= feature.title %></label></li>
    <% end %>
  </ul>
</div>
<div  class="page-wrap">
  <header class="main-header">
    <a href="#filter" class="open-menu button black">več</a>
  </header>
  <a href="#" id="geolocateIcon" class="hidden"><img height="20px" width="20px" src="<%= path_to_image 'locate.svg' %>"></a>
  <div id='map'></div>
</div>
<%= javascript_include_tag 'http://maps.googleapis.com/maps/api/js?sensor=true' %>
<%= javascript_include_tag 'prehrana' %>